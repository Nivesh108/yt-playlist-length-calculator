{
  "name": "playlist-length-calculator-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm run uibuild && node app.js",
    "dev": "nodemon app.js",
    "uibuild": "cd yt-playlist-length-calculator && npm install && npm run build && cd ..",
    "push" : "npm run uibuild && git add . && git commit -m 'UI Build' && git push"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^16.0.1",
    "express": "~4.16.1",
    "googleapis": "^105.0.0",
    "got": "^12.1.0",
    "http-errors": "~1.6.3",
    "jade": "~1.11.0",
    "morgan": "~1.9.1",
    "pipeline": "^0.1.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
